import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  rowTitle: {
    color: "#828282",
    fontSize: 16,
    fontFamily: "SFPRO",
    marginBottom: 16,
  },
  container: {
    marginBottom: 32,
  },
});

export default styles;
