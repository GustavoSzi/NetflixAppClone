import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cover: {
    width: 128,
    height: 190,
    marginRight: 24,
  },
  image: {
    width: "100%",
    height: "100%",
  },
});

export default styles;
